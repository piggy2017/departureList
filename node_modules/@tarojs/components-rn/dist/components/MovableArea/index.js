import * as React from 'react';
import { View } from 'react-native';
class _MovableArea extends React.Component {
    constructor(props) {
        super(props);
        this._onLayout = (event) => {
            const { width, height } = event.nativeEvent.layout;
            const child = React.cloneElement(this.props.children, { layout: { width, height } });
            this.setState({
                width,
                height,
                child
            });
        };
        this.state = {
            child: null
        };
    }
    render() {
        const { style, width = 100, height = 100 } = this.props;
        const { child } = this.state;
        return React.createElement(View, { style: [{ height, width, overflow: 'hidden' }, style], onLayout: this._onLayout }, child);
    }
}
_MovableArea.defaultProps = {};
export default _MovableArea;
//# sourceMappingURL=index.js.map