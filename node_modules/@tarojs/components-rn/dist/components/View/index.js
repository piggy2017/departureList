/**
 * ✔ hoverStyle (hover-class)
 * ✘ hover-stop-propagation
 * ✔ hoverStartTime
 * ✔ hoverStayTime
 */
import * as React from 'react';
import { View, Text, } from 'react-native';
import ClickableSimplified from '../ClickableSimplified';
const stringToText = (child, props) => {
    // TODO: 实现小程序中效果
    return (typeof child === 'string' || typeof child === 'number') ? React.createElement(Text, Object.assign({}, props), child) : child;
};
const _View = (props) => {
    // 兼容View中没用Text包裹的文字 防止报错 直接继承props在安卓中文字会消失？？？
    const child = Array.isArray(props.children) ? props.children.map((c, i) => stringToText(c, Object.assign(Object.assign({ key: i }, props), { style: {} }))) : stringToText(props.children, Object.assign(Object.assign({}, props), { style: {} }));
    return (React.createElement(View, Object.assign({ style: props.style }, props), child));
};
_View.displayName = '_View';
export { _View };
export default ClickableSimplified(_View);
//# sourceMappingURL=index.js.map